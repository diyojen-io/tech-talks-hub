import { Components } from '@mui/material';

const inputLabels: Components['MuiInputLabel'] = {
  styleOverrides: {
    root: {
      fontSize: 10,
      textTransform: 'uppercase',
      opacity: 0.8,
    },
  },
};

export default inputLabels;
